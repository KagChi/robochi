{
  "name": "@kagchi/robochi",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '*' dev",
    "test": "bun test",
    "lint": "bun run --filter '*' lint",
    "clean": "bun run --filter '*' clean"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
